@model IEnumerable<CNPMLyThuyet.Model.ChucNang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PhongBanLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCN }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCN }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCN })
        </td>
    </tr>
}

</table>
